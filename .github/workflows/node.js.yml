name: Node.js CI/CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [18.x, 20.x]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      # Example of using a repo variable
      - name: Print repo variable
        run: echo "Hello, my name is $MY_NAME"
        env:
          MY_NAME: ${{ vars.MY_NAME }}

      # Save app version as output
      - id: get-version
        run: echo "version=$(node -p 'require(\"./package.json\").version')" >> $GITHUB_OUTPUT

      - name: Print version
        run: echo "App version is ${{ steps.get-version.outputs.version }}"

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy Node.js App
        run: |
          echo "🚀 Deploying version ${{ needs.build.outputs.version }} to production"
          # Example: rsync, scp, or Docker push command would go here
